const Pool = require('pg').Pool
const pool = new Pool({
    user: "admin",
    host: "localhost",
    database: "ddcattle",
    password: "thebigdawgisawesome",
    port: 5432
})

const SelectHorses = "SELECT * FROM horses ORDER BY name ASC ;";
const SelectHorseByID = "SELECT * FROM horses WHERE id = $1"

module.exports = {SelectHorses, SelectHorseByID, pool} 